# Standalone contribution policy

Each numbered contribution must be understandable and runnable as an independent research module.

Its README must state its standalone purpose, inputs, implemented method, outputs, valid execution command, evaluation evidence, maturity, and limitations before describing optional integrations with other modules.
