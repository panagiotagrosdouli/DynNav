# Action-triggered Gazebo execution status

This file records the execution gate for the frozen action-triggered Gazebo benchmark.

The benchmark is considered comparative evidence only when a completed run produces `results.json` and passes the paired-trial contract. Failed infrastructure runs remain provenance and are not promoted to efficacy evidence.

Current gate: execute and audit the repaired workflow before promoting any Gazebo numerical result into the manuscript.
