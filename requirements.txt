# DynNav Streamlit dashboard runtime.
# Streamlit Community Cloud installs this file from the repository root.
# Installing the local project is required because dynnav_dashboard uses a src/ layout.
-e .[dashboard]

# Contribution 01 uses PyTorch for learned-heuristic checkpoint inference.
torch>=2.0
