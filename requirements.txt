# Streamlit Community Cloud installs this file from the repository root.
# Install the local src-layout package together with its dashboard extras so
# ``dynnav`` and ``dynnav_dashboard`` are importable at runtime.
-e .[dashboard]

# Contribution 01 — learned A* heuristic checkpoint inference.
torch>=2.0
