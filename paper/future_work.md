# Future Work

Future work should prioritize empirical validation and calibration. The next research steps are to compare against classical and ROS 2 Nav2 baselines, evaluate risk calibration on real sensor logs, extend scenarios to dynamic obstacles, and integrate the deterministic core with a full robot middleware stack. Formal verification of runtime monitors and hardware experiments should be reported only when raw logs, environment manifests, and reproducible trial protocols are included.
