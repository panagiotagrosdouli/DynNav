[project]
name = "dynnav"
version = "0.4.0"
description = "Synthetic dynamic navigation and rerouting research prototype"
requires-python = ">=3.10"
dependencies = ["numpy", "matplotlib", "imageio", "imageio-ffmpeg", "pyyaml"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
